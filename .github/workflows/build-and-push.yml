name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build-prod:
    uses: ${{ vars.WORKFLOWS_GH_USER }}/${{ vars.WORKFLOWS_REPO }}/.github/workflows/build-and-push.yml@main
    with:
      image_name: 'eks-agent'
      version_prefix: ''
