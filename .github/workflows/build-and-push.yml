name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build-prod:
    uses: alvaroc-code/workflow-templates/.github/workflows/build-and-push.yml@main
    with:
      image_name: 'eks-agent'
      version_prefix: ''
